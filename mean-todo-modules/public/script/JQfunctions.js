$(function() {
    $( "#mainbox" ).sortable();
    $( "#mainbox" ).disableSelection();
  });



/*
var colors = ['red', 'blue', 'green', 'gray', 'black', 'yellow'];
$(function(){
     $('.testhoofd').each(function(){
        var randomColor = Math.floor(Math.random() * colors.length)
        $(this).css('background-color', colors[randomColor])
                                  });
});
*/